# DRC- 저장소 작업 지침

## 절대 규칙 — 작업 결과 옵시디언 저장

**모든 작업 결과는 턴 종료 전 반드시 사용자의 옵시디언 볼트에 저장한다. 사용자가 요청하지 않아도 저장한다. 예외 없음.**

저장하지 않고 턴을 종료하는 것은 작업 미완료로 간주한다.

### 저장 위치

Google Drive 커넥터(`mcp__Google_Drive__create_file`)를 사용해 아래 폴더에 마크다운으로 생성한다.
경로는 Google Drive → `옵시디언 / 제2의 뇌 /`.

| 용도 | 폴더 | Drive 폴더 ID |
|---|---|---|
| 질문에 대한 답변·전략·검토 결과 | `wiki/분석/` | `12X1SSfxLUU4_iX0_I3ObxVjVlJfVQOfR` |
| 회사·브랜드·제품·인물·기관 | `wiki/개체/` | `1Al8nLZIPptz9g-OdBz1desFBHT9EyATG` |
| 규제·제도·이론·시장 개념 | `wiki/개념/` | `1whrhO_UEhpvCzQ0E7Y4NN1LBaRZQtfGG` |
| 원본 소스 요약 | `wiki/소스/` | `1JajeT4ymwGyhFKxmT8rvxip5UBZFSLR0` |
| 원본 파일 (AI 수정 금지) | `raw/` | `1xY2nU_7c9ZvRAnFwRiffhlZdYAB_m5uE` |
| 볼트 루트 (CLAUDE.md, 홈.md, index.md, log.md) | — | `1N54XxAeh7taJFut5_GHD7wJVRj-MvXrY` |

`create_file` 호출 시 `contentMimeType: "text/markdown"`, `disableConversionToGoogleType: true`를 반드시 지정한다. 지정하지 않으면 Google Docs로 변환되어 옵시디언에서 읽히지 않는다.

### 실행 절차

1. 작업이 끝나면 결과물의 성격을 판단한다. 질문에 대한 답이면 `분석`, 고유명사 정보면 `개체`, 제도·개념이면 `개념`이다.
2. 아래 작성 규격으로 페이지를 생성한다.
3. 등장한 고유명사·개념 중 재사용 가치가 있는 것은 파생 페이지로 만들고 상호 링크한다.
4. 사용자에게 저장 위치와 파일명을 한 줄로 보고한다.

### 작성 규격

볼트 루트의 `CLAUDE.md`(Drive 파일 ID `1BcBg_hCAzIpgScHL2NGQMeyhsB56UHDZ`)가 정본이다. 핵심은 다음과 같다.

- 모든 페이지는 YAML frontmatter로 시작한다 (Dataview 쿼리용).
- **위키백과체로 쓴다.** 대화체, "제가 정리했습니다" 같은 표현 금지. 사실 서술 중심.
- 이모지는 정해진 마커(`⚠️`) 외 사용 금지.
- **모든 주장에 출처를 단다.** 형식은 `([[소스/파일명]])`. 출처 없는 주장은 `⚠️ 추정` 표시.
- 기존 서술과 충돌하는 내용은 지우지 말고 `## ⚠️ 상충 기록`에 양쪽을 남긴다.
- 한국어로 쓴다. 파일명도 한국어. 고유명사만 원어 병기.
- 내부 링크는 위키링크 `[[페이지명]]` 사용. 모든 페이지는 최소 2개의 링크를 갖는다.

파일명 규칙:

- 분석: `YYYY-MM-DD_질문요약.md`
- 개체·개념: 대상 이름 그대로 (예: `단색.md`, `PFAS 규제.md`)
- 공백 허용. `/ \ : * ? " < > |` 금지.

타입별 템플릿:

```markdown
---
type: 분석
질문: "원 질문 그대로"
날짜: YYYY-MM-DD
tags: [분석]
---

# 질문
## 결론
## 근거
## 반대 관점 / 리스크
## 다음 액션
## 참조한 페이지
```

```markdown
---
type: 개체
분류: 기업 | 인물 | 제품 | 기관 | 브랜드
tags: [개체]
갱신일: YYYY-MM-DD
소스수: N
---

# 이름
## 정체
## 핵심 사실
## 타임라인
## 우리와의 관계
## ⚠️ 상충 기록
## 관련 페이지
```

```markdown
---
type: 개념
tags: [개념]
갱신일: YYYY-MM-DD
소스수: N
---

# 개념명
## 정의
## 왜 중요한가
## 구성 요소 / 작동 방식
## 근거 데이터
## 현재 나의 결해
## ⚠️ 미해결 질문
## 관련 페이지
```

### 알려진 제약

- Google Drive 커넥터에는 파일 수정 도구가 없고 생성만 가능하다. `index.md`·`log.md` 갱신이 필요하면 `download_file_content`로 기존 내용을 읽어 새 내용을 합쳐 다시 생성하거나, 사용자에게 수동 갱신을 요청한다.
- 훅(hook)은 셸 스크립트라 Google Drive에 직접 쓸 수 없다. 이 지침은 훅이 아니라 규칙 기반으로 실행된다.

---

## 사업 도메인

디알씨(DRC)의 사업 영역은 다음과 같다. 답변 시 이 맥락을 전제한다.

- 의료기기·복지용구
- 온라인 판매 (위탁판매·구매대행)
- 화장품·건강기능식품
- 요실금팬티·생리팬티 (시니어케어 및 여성용품)
- 쇼핑 쇼츠·어필리에이트 마케팅

시장 규모·경쟁사·규제·유통채널·단가가 같은 수치는 **출처를 최우선으로 보존**한다. 사용자가 묻지 않아도 리스크와 기회를 선제적으로 제기한다.

---

## 저장소 구조

```
DRC-/
├── CLAUDE.md    ← 이 파일
├── README.md
└── docs/        ← 전략·조사 문서
```

## 진행 중인 작업

| 문서 | 내용 |
|---|---|
| `docs/period-underwear-market-entry-strategy.md` | 생리팬티 한국·미국 동시 진출 전략. 규제 지형, 경쟁 분석, GTM, 24개월 로드맵 |

## Git

- 지정 브랜치에서 작업하고 완료 시 `git push -u origin <branch>`로 푸시한다.
- 커밋 메시지는 무엇을 왜 바꿨는지 서술한다.
