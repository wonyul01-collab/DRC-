# Windows에서 클론해도 셸 스크립트가 LF로 유지되게 합니다.
# CRLF로 체크아웃되면 WSL/Linux에서 "bad interpreter" 로 실행이 실패합니다.
*.sh   text eol=lf
*.py   text eol=lf
*.yaml text eol=lf
*.md   text eol=lf

# PowerShell 스크립트는 Windows 줄바꿈으로 둡니다.
*.ps1  text eol=crlf
